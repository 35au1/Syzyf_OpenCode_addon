# 1. Task to perform

Access `C:\Users\.....\OneDrive\Pulpit\EOPROXY052026`. This is a game proxy and it has logged game
packets. Write a packet library in `packet_library.md` in this folder, keyed by action-family bytes
(the first two), defining every value in every packet. The proxy has full documentation in that
folder; use it. Be precise. Define what the packets actually contain.
Try to store information about packet in table/matrix way for easier comparison between packets.
Historical/current progress file is at "C:\Users\.....\OneDrive\Pulpit\AI loop\dod-loop\_local\packet_library.md"

# 2. Definition of Done

Work is not done if there are still any packets with unidentified information
e.g. unknown bytes purpose. Be creative while looking for each byte purpose, analyze triggers, nearby packets,
compare packets etc. Only if many creative ways are used, we may mark completion for certain packet.

But all packetss need to have definition of all packets. Skipping these is unacceptable
